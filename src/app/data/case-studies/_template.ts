// ==========================================================================
// ⚠️ ไฟล์นี้เป็น "แม่แบบ" (template) เท่านั้น — ห้ามลงทะเบียนไฟล์นี้ใน index.ts
// ห้ามลบไฟล์นี้ทิ้ง ใช้เป็นต้นแบบตอนจะเพิ่มโปรเจกต์ใหม่เท่านั้น
//
// วิธีใช้:
// 1. Copy ไฟล์นี้ไปตั้งชื่อใหม่ในโฟลเดอร์เดียวกัน (เช่น my-new-project.ts)
//    ตั้งชื่อไฟล์ให้สื่อถึง id ที่จะใช้ จะได้หาง่าย
// 2. เปลี่ยนชื่อตัวแปรที่ export จาก `template` เป็นชื่ออื่น (เช่น `myNewProject`)
// 3. กรอกเนื้อหาแทนที่ "" หรือ [] ตาม comment ในไฟล์
// 4. ไปเปิด index.ts ในโฟลเดอร์นี้ เพิ่ม import + เพิ่มเข้า object caseStudies
//    (มีคำอธิบายละเอียดไว้ในไฟล์ index.ts แล้ว)
// 5. เพิ่ม entry คู่กันใน src/app/data/projects.ts ด้วย (ดู NEXT-STEPS.md)
// ==========================================================================

import type { DataDrivenCaseStudy } from "./types";

export const template: DataDrivenCaseStudy = {
  title: "", // หัวข้อใหญ่ในหน้า Hero ของ case study + ชื่อที่โชว์ตอนเป็น "Next Project" ของโปรเจกต์ก่อนหน้า — บังคับ
  subtitle: "", // บรรทัดรองใต้หัวข้อใหญ่ใน Hero (1 ประโยคสั้นๆ) — บังคับ
  category: "", // ข้อความเล็กเหนือหัวข้อใหญ่ใน Hero (เช่น "Web Application") — บังคับ ใส่อะไรก็ได้ไม่ผูกกับ filter ของหน้า Work
  tags: [], // แถบคำ (Tag) ในช่อง "Project Info" ใต้ Hero — บังคับ (ใส่อย่างน้อย 1 คำ) ใส่กี่คำก็ได้ เช่น ["SaaS", "Dashboard"]
  year: "", // ช่อง "Year" ใน grid ข้อมูลโปรเจกต์ — บังคับ
  client: "", // ช่อง "Client" — บังคับ
  role: "", // ช่อง "Role" — บังคับ
  duration: "", // ช่อง "Duration" — บังคับ
  hero: "", // รูปพื้นหลังเต็มจอของ Hero — บังคับ วาง URL รูปได้เลย (เช่นจาก unsplash.com) ถ้าจะใช้รูปอัปโหลดเองต้องให้ Claude Code ช่วย import
  overview: "", // ย่อหน้าเดียวใน section "Overview" — บังคับ (section นี้โชว์เสมอ ไม่ใช่ optional)

  // ------------------------------------------------------------------------
  // ทุก section ด้านล่างนี้เป็น OPTIONAL — ถ้าไม่ต้องการ section ไหน ลบทั้งก้อน
  // ตั้งแต่ชื่อ key (เช่น "screenshots:") ไปจนถึง "]," หรือ "}," ที่ปิดก้อนนั้น
  // ทิ้งได้เลย ไม่ต้องคอมเมนต์ปิดไว้เฉยๆ — TypeScript จะไม่ error เพราะ field
  // พวกนี้ประกาศเป็น optional ไว้แล้วใน types.ts
  // ------------------------------------------------------------------------

  // Gallery รูป + caption ใน section "Screenshots" — ลบทิ้งได้ถ้าไม่มีรูป
  // ใส่ได้กี่รูปก็ได้ รูปแบบแต่ละรายการ: { src: "URL รูป", caption: "คำบรรยายใต้รูป" }
  // (จะไม่ใส่ caption ก็ได้ ลบ key "caption" ทิ้งจากรายการนั้น)
  screenshots: [],

  // section "Problem" — ลบทั้ง key นี้ทิ้งได้ถ้าไม่ต้องการ section นี้
  problem: {
    title: "", // หัวข้อของ section
    description: "", // ย่อหน้านำ
    challenges: [], // list ที่มีเลข badge สีเขียวมะนาวหน้าแต่ละข้ออัตโนมัติ (1,2,3...) — ใส่เป็น string เดี่ยวๆ ทีละข้อ ใส่ได้กี่ข้อก็ได้
  },

  // section "Research" — ลบทั้ง key นี้ทิ้งได้ถ้าไม่ต้องการ section นี้
  research: {
    title: "",
    description: "",
    insights: [], // การ์ดข้อความ จัดเป็น 2 คอลัมน์บนจอใหญ่ — ใส่เป็น string เดี่ยวๆ ทีละข้อ ใส่ได้กี่ข้อก็ได้ (แนะนำเลขคู่จะได้จัด 2 คอลัมน์พอดี)
  },

  // section "Solution" — ลบทั้ง key นี้ทิ้งได้ถ้าไม่ต้องการ section นี้
  solution: {
    title: "",
    description: "",
    features: [], // ใส่ได้กี่รายการก็ได้ รูปแบบแต่ละรายการ: { title: "หัวข้อ feature", description: "คำอธิบาย" }
  },

  // section "Impact" — ลบทั้ง key นี้ทิ้งได้ถ้าไม่ต้องการ section นี้
  impact: {
    title: "",
    metrics: [], // จัด grid 4 ช่องพอดีถ้าใส่ 4 รายการ แต่ใส่มาก/น้อยกว่าได้ รูปแบบแต่ละรายการ: { value: "ตัวเลขใหญ่ เช่น 85%", label: "คำอธิบายเล็กใต้ตัวเลข" }
  },

  nextProject: "", // id ของโปรเจกต์ถัดไปที่จะโชว์เป็นปุ่ม "Next Project" ท้ายหน้า — ต้องตรงกับ key อื่นใน index.ts เช่น "smart-living" — ลบทิ้งได้ถ้าไม่มีโปรเจกต์ถัดไป (หรือใส่ id ที่ published: false ปุ่มจะไม่โชว์เองอัตโนมัติ)
};
