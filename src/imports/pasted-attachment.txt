<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Design System — Portfolio Case Study</title>
<link href="https://fonts.googleapis.com/css2?family=Syne:wght@400;600;700;800&family=DM+Sans:ital,wght@0,300;0,400;0,500;1,300&display=swap" rel="stylesheet">
<style>
  :root {
    --yellow: #FFD233;
    --red: #E3000B;
    --blue: #006DB7;
    --black: #0D0D0D;
    --white: #F9F6F0;
    --gray: #8A8A8A;
    --light: #F0EDE6;
    --accent: #FF6B35;
  }

  * { margin: 0; padding: 0; box-sizing: border-box; }

  html { scroll-behavior: smooth; }

  body {
    background: var(--black);
    color: var(--white);
    font-family: 'DM Sans', sans-serif;
    font-size: 16px;
    line-height: 1.6;
    overflow-x: hidden;
  }

  /* ── NOISE OVERLAY ── */
  body::before {
    content: '';
    position: fixed;
    inset: 0;
    background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)' opacity='0.04'/%3E%3C/svg%3E");
    pointer-events: none;
    z-index: 9999;
    opacity: 0.4;
  }

  h1, h2, h3, h4 { font-family: 'Syne', sans-serif; }

  /* ── HERO ── */
  .hero {
    min-height: 100vh;
    display: grid;
    grid-template-columns: 1fr 1fr;
    position: relative;
    overflow: hidden;
  }

  .hero-left {
    padding: 80px 60px;
    display: flex;
    flex-direction: column;
    justify-content: center;
    position: relative;
    z-index: 2;
  }

  .hero-right {
    position: relative;
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
  }

  .hero-tag {
    display: inline-block;
    background: var(--yellow);
    color: var(--black);
    font-family: 'Syne', sans-serif;
    font-size: 11px;
    font-weight: 700;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    padding: 6px 14px;
    margin-bottom: 32px;
    width: fit-content;
  }

  .hero h1 {
    font-size: clamp(48px, 6vw, 88px);
    font-weight: 800;
    line-height: 0.95;
    letter-spacing: -0.03em;
    margin-bottom: 32px;
  }

  .hero h1 span.outline {
    -webkit-text-stroke: 2px var(--white);
    color: transparent;
  }

  .hero h1 span.yellow { color: var(--yellow); }

  .hero-desc {
    font-size: 17px;
    color: #BBBBB0;
    max-width: 420px;
    line-height: 1.7;
    margin-bottom: 48px;
    font-weight: 300;
  }

  .hero-meta {
    display: flex;
    gap: 40px;
  }

  .meta-item label {
    font-size: 10px;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: var(--gray);
    display: block;
    margin-bottom: 4px;
  }

  .meta-item span {
    font-family: 'Syne', sans-serif;
    font-weight: 600;
    font-size: 14px;
  }

  /* LEGO BRICKS VISUAL */
  .lego-visual {
    width: 100%;
    height: 100%;
    position: relative;
    background: #111;
  }

  .lego-grid {
    position: absolute;
    inset: 0;
    display: grid;
    grid-template-columns: repeat(6, 1fr);
    grid-template-rows: repeat(8, 1fr);
    gap: 3px;
    padding: 3px;
  }

  .brick {
    border-radius: 2px;
    position: relative;
    transition: transform 0.3s ease, opacity 0.3s ease;
    animation: brickFloat 4s ease-in-out infinite;
  }

  .brick::after {
    content: '';
    position: absolute;
    top: 20%;
    left: 50%;
    transform: translateX(-50%);
    width: 45%;
    height: 30%;
    border-radius: 50%;
    background: rgba(255,255,255,0.18);
  }

  .brick:hover { transform: translateY(-4px) scale(1.05); }

  @keyframes brickFloat {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-3px); }
  }

  .b-yellow { background: var(--yellow); animation-delay: 0s; }
  .b-red    { background: var(--red);    animation-delay: 0.3s; }
  .b-blue   { background: var(--blue);   animation-delay: 0.6s; }
  .b-white  { background: #E8E4DC;       animation-delay: 0.9s; }
  .b-dark   { background: #2A2A2A;       animation-delay: 1.2s; }
  .b-orange { background: var(--accent); animation-delay: 0.15s; }

  .hero-right-label {
    position: absolute;
    bottom: 40px;
    left: 40px;
    font-size: 11px;
    letter-spacing: 0.1em;
    color: rgba(255,255,255,0.3);
    text-transform: uppercase;
    font-family: 'Syne', sans-serif;
  }

  /* ── SECTION WRAPPER ── */
  section { padding: 120px 60px; }
  .section-inner { max-width: 1200px; margin: 0 auto; }

  .section-label {
    font-size: 11px;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    color: var(--yellow);
    font-family: 'Syne', sans-serif;
    font-weight: 700;
    margin-bottom: 16px;
    display: flex;
    align-items: center;
    gap: 12px;
  }

  .section-label::before {
    content: '';
    display: block;
    width: 24px;
    height: 2px;
    background: var(--yellow);
  }

  .section-title {
    font-size: clamp(36px, 4vw, 56px);
    font-weight: 800;
    line-height: 1.05;
    letter-spacing: -0.02em;
    margin-bottom: 48px;
  }

  /* ── STORY SECTION ── */
  .story {
    background: var(--black);
    border-top: 1px solid #1E1E1E;
  }

  .story-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 80px;
    align-items: center;
  }

  .story-text p {
    color: #BBBBB0;
    font-size: 17px;
    line-height: 1.8;
    font-weight: 300;
    margin-bottom: 20px;
  }

  .story-text p strong {
    color: var(--white);
    font-weight: 500;
  }

  /* ANALOGY CARDS */
  .analogy-stack {
    display: flex;
    flex-direction: column;
    gap: 16px;
  }

  .analogy-card {
    background: #161616;
    border: 1px solid #2A2A2A;
    border-radius: 4px;
    padding: 24px 28px;
    display: grid;
    grid-template-columns: 48px 1fr;
    gap: 20px;
    align-items: start;
    transition: border-color 0.3s, transform 0.3s;
    cursor: default;
  }

  .analogy-card:hover {
    border-color: var(--yellow);
    transform: translateX(6px);
  }

  .analogy-icon {
    width: 48px;
    height: 48px;
    border-radius: 3px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 22px;
    flex-shrink: 0;
  }

  .analogy-card h4 {
    font-size: 15px;
    font-weight: 700;
    margin-bottom: 6px;
    letter-spacing: -0.01em;
  }

  .analogy-card p {
    font-size: 14px;
    color: #888;
    line-height: 1.6;
  }

  .analogy-card .arrow {
    font-size: 11px;
    letter-spacing: 0.1em;
    color: var(--yellow);
    margin-top: 8px;
    display: block;
    text-transform: uppercase;
  }

  /* ── FLOW SECTION ── */
  .flow-section {
    background: #0A0A0A;
    border-top: 1px solid #1E1E1E;
  }

  .flow-steps {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 2px;
    margin-top: 16px;
  }

  .flow-step {
    background: #111;
    padding: 48px 40px;
    position: relative;
    overflow: hidden;
    transition: background 0.3s;
  }

  .flow-step:hover { background: #161616; }

  .flow-step::before {
    content: attr(data-num);
    position: absolute;
    top: -10px;
    right: 24px;
    font-family: 'Syne', sans-serif;
    font-size: 120px;
    font-weight: 800;
    color: #1A1A1A;
    line-height: 1;
    pointer-events: none;
    transition: color 0.3s;
  }

  .flow-step:hover::before { color: #222; }

  .step-tag {
    display: inline-block;
    padding: 4px 12px;
    font-size: 10px;
    letter-spacing: 0.12em;
    font-family: 'Syne', sans-serif;
    font-weight: 700;
    text-transform: uppercase;
    border-radius: 2px;
    margin-bottom: 20px;
  }

  .step-tag.token  { background: rgba(255,210,51,0.15); color: var(--yellow); }
  .step-tag.system { background: rgba(0,109,183,0.15);  color: #5BB8FF; }
  .step-tag.comp   { background: rgba(255,107,53,0.15); color: var(--accent); }

  .flow-step h3 {
    font-size: 26px;
    font-weight: 800;
    margin-bottom: 16px;
    letter-spacing: -0.02em;
    position: relative;
  }

  .flow-step p {
    font-size: 14px;
    color: #888;
    line-height: 1.7;
    position: relative;
  }

  .flow-step ul {
    list-style: none;
    margin-top: 20px;
    display: flex;
    flex-direction: column;
    gap: 8px;
    position: relative;
  }

  .flow-step ul li {
    font-size: 13px;
    color: #666;
    padding-left: 16px;
    position: relative;
  }

  .flow-step ul li::before {
    content: '—';
    position: absolute;
    left: 0;
    color: #333;
  }

  .flow-arrow {
    display: flex;
    align-items: center;
    justify-content: center;
    background: #0D0D0D;
    width: 2px;
    position: relative;
  }

  /* ── CHALLENGE / SOLUTION ── */
  .cs-section {
    background: var(--black);
    border-top: 1px solid #1E1E1E;
  }

  .cs-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 2px;
  }

  .cs-col {
    padding: 56px 48px;
  }

  .cs-col.challenge { background: #0E0E0E; }
  .cs-col.solution  { background: #121212; }

  .cs-col-label {
    font-size: 10px;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    font-family: 'Syne', sans-serif;
    font-weight: 700;
    margin-bottom: 32px;
    padding-bottom: 16px;
    border-bottom: 2px solid;
  }

  .challenge .cs-col-label { color: var(--red); border-color: var(--red); }
  .solution  .cs-col-label { color: #4ADE80;     border-color: #4ADE80; }

  .cs-item {
    margin-bottom: 32px;
    padding-bottom: 32px;
    border-bottom: 1px solid #1E1E1E;
  }

  .cs-item:last-child { border-bottom: none; margin-bottom: 0; padding-bottom: 0; }

  .cs-item-tag {
    font-size: 10px;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    font-family: 'Syne', sans-serif;
    color: var(--gray);
    margin-bottom: 10px;
  }

  .cs-item h4 {
    font-size: 17px;
    font-weight: 700;
    margin-bottom: 10px;
    line-height: 1.3;
  }

  .cs-item p {
    font-size: 14px;
    color: #777;
    line-height: 1.7;
  }

  /* ── OUTCOME ── */
  .outcome-section {
    background: var(--yellow);
    color: var(--black);
    border-top: none;
  }

  .outcome-section .section-label { color: var(--black); opacity: 0.5; }
  .outcome-section .section-label::before { background: var(--black); }
  .outcome-section .section-title { color: var(--black); }

  .outcome-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 32px;
    margin-top: 16px;
  }

  .outcome-card {
    background: rgba(0,0,0,0.08);
    padding: 36px 32px;
    border-radius: 4px;
  }

  .outcome-num {
    font-family: 'Syne', sans-serif;
    font-size: 52px;
    font-weight: 800;
    line-height: 1;
    margin-bottom: 8px;
    letter-spacing: -0.03em;
  }

  .outcome-card h4 {
    font-size: 16px;
    font-weight: 700;
    margin-bottom: 8px;
  }

  .outcome-card p {
    font-size: 14px;
    color: rgba(0,0,0,0.6);
    line-height: 1.6;
  }

  /* ── QUOTE ── */
  .quote-band {
    background: #0A0A0A;
    border-top: 1px solid #1E1E1E;
    padding: 80px 60px;
    text-align: center;
  }

  .quote-band blockquote {
    font-family: 'Syne', sans-serif;
    font-size: clamp(24px, 3vw, 40px);
    font-weight: 700;
    line-height: 1.3;
    max-width: 800px;
    margin: 0 auto;
    letter-spacing: -0.02em;
  }

  .quote-band blockquote em {
    font-style: normal;
    color: var(--yellow);
  }

  /* ── DIVIDER LINE ── */
  .divider {
    height: 1px;
    background: linear-gradient(90deg, transparent, #333, transparent);
    margin: 0 60px;
  }

  /* ── SCROLL ANIMATIONS ── */
  .reveal {
    opacity: 0;
    transform: translateY(30px);
    transition: opacity 0.7s ease, transform 0.7s ease;
  }

  .reveal.visible {
    opacity: 1;
    transform: translateY(0);
  }

  .reveal-delay-1 { transition-delay: 0.1s; }
  .reveal-delay-2 { transition-delay: 0.2s; }
  .reveal-delay-3 { transition-delay: 0.3s; }

  /* ── FOOTER ── */
  footer {
    padding: 48px 60px;
    border-top: 1px solid #1E1E1E;
    display: flex;
    justify-content: space-between;
    align-items: center;
    font-size: 13px;
    color: #444;
  }

  footer span { font-family: 'Syne', sans-serif; font-weight: 700; color: #666; }
</style>
</head>
<body>

<!-- ══ HERO ══ -->
<section class="hero" style="padding:0">
  <div class="hero-left">
    <div class="hero-tag">Case Study — Design System</div>
    <h1>
      Building<br>
      <span class="outline">Blocks</span><br>
      That <span class="yellow">Scale.</span>
    </h1>
    <p class="hero-desc">
      Building a design system from scratch on a fast-moving project — 
      where the brand identity was still evolving and deadlines weren't waiting.
    </p>
    <div class="hero-meta">
      <div class="meta-item">
        <label>Role</label>
        <span>UX/UI Designer</span>
      </div>
      <div class="meta-item">
        <label>Scope</label>
        <span>Design System · Tokens · Components</span>
      </div>
      <div class="meta-item">
        <label>Type</label>
        <span>New Product Build</span>
      </div>
    </div>
  </div>

  <div class="hero-right">
    <div class="lego-visual">
      <div class="lego-grid" id="legoGrid"></div>
    </div>
    <div class="hero-right-label">Design Tokens → Components</div>
  </div>
</section>

<!-- ══ LEGO STORY ══ -->
<section class="story">
  <div class="section-inner">
    <div class="story-grid">
      <div class="story-text reveal">
        <div class="section-label">The Analogy</div>
        <h2 class="section-title">Think of it<br>like LEGO.</h2>
        <p>
          Every LEGO set starts with the same foundation — <strong>small, standardized bricks.</strong>
          On their own, they're just colored plastic. But assembled with intention,
          they become anything: a house, a spaceship, a city.
        </p>
        <p>
          A Design System works the same way. <strong>Design Tokens</strong> are the raw bricks —
          your colors, spacing, and typography values. Stack them into rules, and you get a 
          <strong>Design System</strong>. Combine those rules into reusable patterns, and you 
          get <strong>Components</strong> — buttons, inputs, modals — ready to build any interface.
        </p>
        <p>
          The beauty? <strong>Change one brick, and the whole structure updates.</strong>
          That's not just good design — that's how you survive a project where 
          everything is still being figured out.
        </p>
      </div>

      <div class="analogy-stack reveal reveal-delay-1">
        <div class="analogy-card">
          <div class="analogy-icon" style="background:rgba(255,210,51,0.12)">🧱</div>
          <div>
            <h4>Design Token = The Brick</h4>
            <p>The atomic value. Color, spacing, radius, font size. Nothing is hardcoded — everything references a token.</p>
            <span class="arrow">→ e.g. color-primary: #FFD233</span>
          </div>
        </div>
        <div class="analogy-card">
          <div class="analogy-icon" style="background:rgba(0,109,183,0.12)">🏗️</div>
          <div>
            <h4>Design System = The Blueprint</h4>
            <p>Rules for how bricks connect. Typography scale, spacing grid, elevation — the logic layer that brings order.</p>
            <span class="arrow">→ e.g. button-padding: spacing-md</span>
          </div>
        </div>
        <div class="analogy-card">
          <div class="analogy-icon" style="background:rgba(255,107,53,0.12)">🏠</div>
          <div>
            <h4>Component = The Finished Build</h4>
            <p>Assembled, reusable pieces. Button, Input Field, Modal — built once, used everywhere, consistent always.</p>
            <span class="arrow">→ e.g. &lt;PrimaryButton /&gt;</span>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- ══ FLOW ══ -->
<section class="flow-section" style="padding-top:0; padding-left:0; padding-right:0; padding-bottom:120px">
  <div class="section-inner" style="max-width:100%; padding: 0 60px 0">
    <div style="padding-top:120px; margin-bottom:48px">
      <div class="section-label reveal">The Process</div>
      <h2 class="section-title reveal">From zero<br>to a system.</h2>
    </div>
  </div>
  <div class="flow-steps">
    <div class="flow-step reveal" data-num="01">
      <div class="step-tag token">Layer 01</div>
      <h3>Design Tokens</h3>
      <p>The foundation of everything. Every visual decision lives here as a named variable — not a hardcoded value.</p>
      <ul>
        <li>Color palette (primary, semantic, neutral)</li>
        <li>Typography scale & font families</li>
        <li>Spacing & sizing units</li>
        <li>Border radius & elevation</li>
        <li>Mapped to CSS variables / Figma variables</li>
      </ul>
    </div>
    <div class="flow-step reveal reveal-delay-1" data-num="02">
      <div class="step-tag system">Layer 02</div>
      <h3>Design System</h3>
      <p>Tokens become rules. Rules become patterns. This layer defines how elements relate to each other.</p>
      <ul>
        <li>Grid & layout structure</li>
        <li>State definitions (default, hover, disabled, error)</li>
        <li>Interaction patterns & feedback</li>
        <li>Iconography & illustration standards</li>
        <li>Documentation for Dev handoff</li>
      </ul>
    </div>
    <div class="flow-step reveal reveal-delay-2" data-num="03">
      <div class="step-tag comp">Layer 03</div>
      <h3>Components</h3>
      <p>Rules assembled into fully functional, reusable UI building blocks — aligned to the tech stack used by Dev.</p>
      <ul>
        <li>Buttons, inputs, selects, checkboxes</li>
        <li>Cards, modals, drawers, toasts</li>
        <li>Navigation & table patterns</li>
        <li>Variants for each component state</li>
        <li>Dev-ready specs with token references</li>
      </ul>
    </div>
  </div>
</section>

<!-- ══ CHALLENGE / SOLUTION ══ -->
<section class="cs-section" style="padding:0">
  <div class="section-inner" style="max-width:100%; padding: 80px 60px 0">
    <div class="section-label reveal">The Hard Part</div>
    <h2 class="section-title reveal">Challenges<br>& How We Solved Them.</h2>
  </div>
  <div class="cs-grid" style="margin-top:48px">
    <div class="cs-col challenge">
      <div class="cs-col-label">⚡ Challenges</div>

      <div class="cs-item reveal">
        <div class="cs-item-tag">Design · Brand Identity</div>
        <h4>CI was never final</h4>
        <p>The brand identity kept changing — fonts, colors, and styles were still being refined while design work was already underway. Every update threatened to break what was already built.</p>
      </div>

      <div class="cs-item reveal reveal-delay-1">
        <div class="cs-item-tag">Design · Timeline</div>
        <h4>Building while shipping</h4>
        <p>The project was moving fast with no runway. Design system and product features had to be developed in parallel — there was no "phase 1 system, phase 2 product" luxury.</p>
      </div>

      <div class="cs-item reveal reveal-delay-2">
        <div class="cs-item-tag">Developer Collaboration</div>
        <h4>Components had to match the tech stack</h4>
        <p>If the component structure didn't align with how Dev was building, implementation would slow down or create inconsistencies between design and production.</p>
      </div>
    </div>

    <div class="cs-col solution">
      <div class="cs-col-label">✦ Solutions</div>

      <div class="cs-item reveal">
        <div class="cs-item-tag">Solution · Token-First Approach</div>
        <h4>Abstract everything into tokens</h4>
        <p>Instead of hardcoding values, every color and font reference pointed to a token. When CI changed, we updated the token — not every component individually. One change, everywhere updated.</p>
      </div>

      <div class="cs-item reveal reveal-delay-1">
        <div class="cs-item-tag">Solution · Parallel Workflow</div>
        <h4>Base system + feature-level system running together</h4>
        <p>We maintained a core base system while simultaneously building feature-specific component sets. Both tracks were designed to stay in sync, so neither blocked the other from progressing.</p>
      </div>

      <div class="cs-item reveal reveal-delay-2">
        <div class="cs-item-tag">Solution · Dev-Aligned Components</div>
        <h4>Design that speaks the same language as Dev</h4>
        <p>Components were scoped and named to match what the tech framework supported. Regular syncs ensured design didn't create patterns that were difficult or impossible to implement cleanly.</p>
      </div>
    </div>
  </div>
</section>

<!-- ══ QUOTE ══ -->
<div class="quote-band">
  <blockquote class="reveal">
    "The goal wasn't perfection. It was <em>flexibility</em> — 
    a system that could absorb change without falling apart."
  </blockquote>
</div>

<!-- ══ OUTCOME ══ -->
<section class="outcome-section">
  <div class="section-inner">
    <div class="section-label">The Impact</div>
    <h2 class="section-title">What the system<br>made possible.</h2>
    <div class="outcome-grid">
      <div class="outcome-card reveal">
        <div class="outcome-num">CI</div>
        <h4>Change-Resilient Foundation</h4>
        <p>Brand updates required only token changes — no need to rework individual components or screens.</p>
      </div>
      <div class="outcome-card reveal reveal-delay-1">
        <div class="outcome-num">×2</div>
        <h4>Faster Design Iterations</h4>
        <p>Reusable components meant new feature screens could be assembled quickly without redesigning from scratch.</p>
      </div>
      <div class="outcome-card reveal reveal-delay-2">
        <div class="outcome-num">✦</div>
        <h4>Consistent End-User Experience</h4>
        <p>Admins using the product encountered the same patterns across all features — familiar, learnable, and reliable.</p>
      </div>
    </div>
  </div>
</section>

<!-- ══ FOOTER ══ -->
<footer>
  <span>Design System — Portfolio Case Study</span>
  <span style="color:#333">Token → System → Component</span>
</footer>

<script>
  // ── LEGO GRID ──
  const colors = ['b-yellow','b-yellow','b-red','b-blue','b-blue','b-white','b-dark','b-dark','b-dark','b-orange'];
  const grid = document.getElementById('legoGrid');
  for (let i = 0; i < 48; i++) {
    const brick = document.createElement('div');
    const c = colors[Math.floor(Math.random() * colors.length)];
    brick.className = 'brick ' + c;
    brick.style.animationDelay = (Math.random() * 3) + 's';
    grid.appendChild(brick);
  }

  // ── SCROLL REVEAL ──
  const observer = new IntersectionObserver((entries) => {
    entries.forEach(e => {
      if (e.isIntersecting) {
        e.target.classList.add('visible');
        observer.unobserve(e.target);
      }
    });
  }, { threshold: 0.12 });

  document.querySelectorAll('.reveal').forEach(el => observer.observe(el));
</script>
</body>
</html>